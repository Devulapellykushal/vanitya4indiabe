const config = require('./config');
const middleware = require('./middleware');
const exceptions = require('./exceptions');

/**
 * Core module exports
 */
module.exports = {
  config,
  middleware,
  exceptions
};